# Echo Move 0.0.20

本次版本主要围绕统计月报、第三方数据渠道和本地存储目录管理继续完善。月度数据可以直接生成适合分享的高清海报，第三方同步在写入本地资料库前有更明确的确认流程，地图包与 3D 地图缓存也可以迁移到非系统盘。

## 本次发布亮点

- 统计页月度数据新增“生成月报”入口，可导出竖版高清 PNG，支持选择展示的统计指标、同步月历设置、显示运动类型分布并缩放平移预览。
- 设置里的品牌管理支持为设备品牌单独设置颜色，活动详情头部和品牌选择器会使用品牌色显示色点，品牌色不再跟随应用主题色变化。
- 第三方平台同步到当前资料库前新增统一确认弹窗，会先展示本次拉取的活动数量和列表，支持单条排除、关联已有活动、覆盖已有活动、批量处理已有活动，并可按单条或全部活动保存第三方文件到本地目录。
- 设置里的第三方数据来源升级为“数据渠道”表格，集中展示 Strava、Garmin 国际、Garmin 中国和迈金 / OnelapFit 的连接状态、账号、能力、最近同步时间和操作入口。
- “数据和存储”页新增地图包目录和 3D 地图缓存目录管理，可查看、打开、迁移、清理或恢复默认目录。

## 主要改进

### 统计与展示

- 月度统计支持生成竖版月报海报，适合保存到本地或分享到社群。
- 月报导出支持配置统计指标、月历展示和运动类型分布，并可在导出前缩放平移预览。

### 第三方数据渠道

- 第三方同步确认弹窗会优先展示上一次获取的活动预览，并提供“重新获取”按钮，减少反复打开弹窗时触发平台接口请求的次数。
- 第三方同步支持导入虚拟骑行和跑步机等无 GPS 活动，会保存为室内 / 虚拟摘要活动；本地保存默认可落为平台摘要 JSON，能拿到原始 FIT / GPX 时仍优先保存原始文件。
- 第三方数据渠道表格中的行内操作改为语义更明确的图标按钮，鼠标移入会展示操作名称，减少操作列占用空间。
- 第三方数据渠道配置弹窗新增信息说明入口，可查看 Strava OAuth、Garmin 服务器区域和迈金 Token 等配置含义与安全注意。
- 第三方活动同步和迈金本地导出新增暂停、继续和取消控制，长任务执行中可在任务摘要里临时暂停或停止后续活动处理。

### 数据与存储

- “数据和存储”页新增地图包目录管理，可直接查看、打开并更改应用瓦片地图包的存储目录，和活动托管目录一起归入可迁移数据。
- “数据和存储”页新增 3D 地图缓存目录迁移，可将 DEM 高程、高清地形网格和 OSM 几何缓存整体迁移到非系统盘，并支持打开、清理和恢复默认目录。

## 修复

- 修复中国区 Garmin Connect 账号连接时可能卡在 Token 交换失败的问题，连接流程会使用中国区对应的 SSO service 与 DI token 参数。
- 修复“数据和存储”页活动托管目录显示为当前用户子目录、但迁移实际作用于托管根目录导致的口径不一致；现在总览会直接显示、复制和打开托管根目录。
- 修复 Windows 上选择与当前存储目录仅大小写不同的路径时，可能被误判为新目录的问题，避免活动托管目录和 3D 地图缓存目录迁移出现误操作。
- 修复迈金 / OnelapFit 数据渠道点击“导出”会打开配置弹窗或直接开始导出的问题，现在会先打开独立导出弹窗确认范围和目录。

## 安装包

`0.0.20` 当前已确认的安装包形态如下：

- macOS (Apple Silicon): `Echo-Move-0.0.20-arm64.dmg`
- macOS (Intel): `Echo-Move-0.0.20-x64.dmg`
- Windows (64-bit): `Echo-Move-0.0.20-setup.exe`
- Linux (AppImage): `Echo-Move-0.0.20-x86_64.AppImage`
- Linux (deb): `echo-move_0.0.20_amd64.deb`

## 已知说明

- macOS 当前仍以“检测更新后打开发布页，手动下载安装”为主；Apple Silicon 设备请选择 `arm64`，Intel 设备请选择 `x64`。
- Linux 用户可优先选择 AppImage 直接运行，Debian / Ubuntu 系发行版也可使用 `deb` 安装包。
- Echo Move 仍采用 local-first 设计，活动数据默认保存在本地，不依赖账号体系或云端同步。

## 下载

- Releases: https://github.com/havebear/echo-move-release/releases
