# Echo Move 0.0.10

本次版本主要围绕活动详情体验继续收口，重点补齐自定义点位、地图回放与三维地形细节能力，并修复一批导入、时间口径和相册预览相关问题。

## 本次发布亮点

- 活动详情新增自定义点位，可在二维/三维视图中添加备注、观景、休息、补水、补给、风险与检查点等标注，并直接定位到轨迹位置。
- 地图显示控制更细，照片点位、自定义点位、关键点显示与标记大小都可以单独调节。
- 二维与三维轨迹回放补充实时数据面板、多指标着色、自动全屏与更平滑的播放链路。
- 三维地形新增 `ultra` 精细度和 DEM 缓存管理，复杂路线下的地形细节与重复查看体验更稳定。

## 主要改进

### 活动详情与地图

- 自定义点位已接入二维 / 三维地图，可用于记录补给点、风险点、检查点和临时备注。
- 关键点、照片点位和自定义点位的层级关系与视觉样式经过收口，缩放、聚焦和回放时更清晰。
- 回放过程中会展示实时轨迹点数据面板，便于结合地图回看速度、爬升等指标变化。

### 三维与相册

- 三维地形新增更高精度档位，并提供 DEM 缓存查看与清理入口。
- 相册缩略图会按 EXIF 方向自动纠正，原图改为按需读取，切换相册与查看大图更流畅。

### 导入与数据口径

- 修复苹果健康导入时的运动类型识别问题。
- 修复部分页面“运动时间 / 全程时间”口径不一致的问题。
- 修复二维轨迹回放可能中断、数据面板中途消失，以及三维分段聚焦与地形穿透相关异常。

## 安装包

`0.0.10` 当前建议关注的安装包形态如下：

- macOS (Apple Silicon): `Echo-Move-0.0.10-arm64.dmg`
- macOS (Intel): `Echo-Move-0.0.10-x64.dmg`
- Windows (64-bit): `Echo-Move-0.0.10-setup.exe`
- Linux (64-bit): `Echo-Move-0.0.10-x64.AppImage`
- Linux (64-bit): 对应 `.deb` 安装包

## 已知说明

- macOS 当前仍以“检测更新后打开发布页，手动下载安装”为主；Apple Silicon 设备请选择 `arm64`，Intel 设备请选择 `x64`。
- Echo Move 仍采用 local-first 设计，活动数据默认保存在本地，不依赖账号体系或云端同步。
- AI Lab 相关能力仍依赖用户在应用内配置可用模型。

## 下载

- Releases: https://github.com/havebear/echo-move-release/releases
