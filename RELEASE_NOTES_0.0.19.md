# Echo Move 0.0.19

本次版本主要围绕三维地形沙盘、路书海拔数据和高密度地图交互继续打磨。高精度 DEM 场景更容易生成和复看，路书、赛段与对比视图的海拔缺口更少，复杂路线拖动也更稳定。

## 本次发布亮点

- 三维地形沙盘新增高精度 DEM 预设，可按路线范围生成更细的高程网格，并在生成前展示网格规模、DEM 来源和预计成本。
- 高精度沙盘支持 IndexedDB 持久化保存，完整高度网格、DEM 元信息和场景配置可长期复看；当前环境无法使用 IndexedDB 时会回退到旧存储方式。
- 活动对比支持使用原始轨迹活动参与，更多从原始文件解析出的活动现在可以进入对比视图。
- AI 路书保存后会自动补齐海拔采样，路书和赛段缺少海拔时会显示可理解提示，减少后续分析、坡度图和赛段详情中的空缺状态。
- 应用瓦片新增内置 `雾白静灰` 样式，提供更接近灰白截图底图的低干扰视觉方案。

## 主要改进

### 三维地形沙盘

- 高精度网格构建状态、场景叠加层构建、方向识别和地形显示经过优化，大场景生成时反馈更清晰，结果也更稳定。
- 沙盘场景会保存完整高度网格、DEM 元信息和关键配置，后续打开时无需重新生成同一份高精度数据。

### 路书、赛段与对比

- 活动对比现在能纳入更多原始轨迹活动，方便把从原始文件解析出的活动放在一起比较。
- AI 路书保存后会尝试补齐海拔采样，缺少海拔数据时会在路书和赛段相关页面给出更清楚的提示。

### 地图与视觉体验

- 地图、路书详情和赛段详情减少首次交互与拖拽中的重渲染，高密度路线浏览更跟手。
- 新增 `雾白静灰` 应用瓦片样式，强化水域、道路骨架和边界线层次，同时默认隐藏地名标注。

## 修复

- 修复部分高密度路书详情地图拖拽时像被限制在小范围、短距离移动后卡住的问题。
- 修复路书列表拖拽导入文件时可能没有反馈的问题。
- 修复活动山脊图填充透明度设为完全不透明时仍会透出底层颜色的问题。

## 安装包

`0.0.19` 当前已确认的安装包形态如下：

- macOS (Apple Silicon): `Echo-Move-0.0.19-arm64.dmg`
- macOS (Intel): `Echo-Move-0.0.19-x64.dmg`
- Windows (64-bit): `Echo-Move-0.0.19-setup.exe`

## 已知说明

- macOS 当前仍以“检测更新后打开发布页，手动下载安装”为主；Apple Silicon 设备请选择 `arm64`，Intel 设备请选择 `x64`。
- Linux 安装包以 Releases 页面实际显示为准。
- 高精度 DEM 沙盘需要读取 DEM 数据源；网络或数据源不可用时，请降低精度或稍后重试。
- Echo Move 仍采用 local-first 设计，活动数据默认保存在本地，不依赖账号体系或云端同步。

## 下载

- Releases: https://github.com/havebear/echo-move-release/releases
